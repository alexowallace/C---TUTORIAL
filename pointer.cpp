#include <iostream>
using namespace std;

int main()
{
    // int a = 3;
    // int *b = &a;
    // cout << "the address of a is " << &a << endl;
    // cout << "the address of a is " << b << endl;
    // cout << "the address of b is " << &b << endl;
    // cout << "the value of b is " << *b << endl;

    // int **c = &b;
    // cout << "the address of b is " << &b << endl;
    // cout << "the address of b is " << c << endl;
    // cout << "the address of c is " << &c << endl;
    // cout << "the value of *c is address of a " << *c << endl;
    // cout << "the value of **c is value of a " << **c << endl;

    // array

    int marks[4] = {23, 45, 56, 76};
    // int i = 0;
    // do
    // {
    //     cout << marks[i] << endl;
    //     i++;

    // } while (i < 4);


    // int *p = marks;
    // int j = 1;

    // do
    // {
    //     cout << *p << endl;
    //     *(p++);
    //     j++;

    // } while (j<5);



    return 0;
}